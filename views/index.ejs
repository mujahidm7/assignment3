<%- include('partials/header') %>

<div class="text-center py-5">
  <h1>Welcome to Assignment Tracker</h1>
  <p class="lead">Track your school assignments easily in one place.</p>

  <% if (!currentUser) { %>
    <a href="/register" class="btn btn-primary mt-3">Get Started</a>
  <% } else { %>
    <a href="/assignments" class="btn btn-primary mt-3">View My Assignments</a>
  <% } %>
</div>

<%- include('partials/footer') %>
